<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MovieApp.Features.Events.Views.BuyTicketPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource AppBackgroundBrush}" Padding="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Margin="0,0,20,0" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12">
            <Image x:Name="PosterImage" Stretch="UniformToFill"/>
        </Border>

        <StackPanel Grid.Column="1" Spacing="12">
            <TextBlock x:Name="TitleText" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap"/>
            <TextBlock x:Name="DescriptionText" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="Date:" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock x:Name="DateText" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="Location:" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock x:Name="LocationText" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>

            <TextBlock Text="Price" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,6,0,0"/>
            <TextBlock x:Name="PriceText" FontSize="18" FontWeight="Bold" Foreground="{StaticResource GoldBrush}"/>

            <Button x:Name="ConfirmButton" Content="Confirm purchase" Click="ConfirmButton_Click"/>

            <TextBlock x:Name="InsufficientText" Foreground="Red" Visibility="Collapsed">Insufficient funds.</TextBlock>
        </StackPanel>
    </Grid>
</Page>
