<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MovieApp.Features.Referrals.Views.ReferralPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MovieApp.DataLayer.Models"
    mc:Ignorable="d">

    <Grid Background="{StaticResource AppBackgroundBrush}" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Referrals" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>

        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,16" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1">
            <StackPanel Spacing="8">
                <TextBlock Text="Your referral code" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                <TextBlock Text="{x:Bind ViewModel.ReferralCode, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource GoldBrush}"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource CardBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,16" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1">
            <StackPanel Spacing="10">
                <TextBlock Text="Apply a friend's code" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBox Width="240" PlaceholderText="Enter code" Text="{x:Bind ViewModel.CodeInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Content="Apply" Command="{x:Bind ViewModel.ApplyCodeCommand}" Background="{StaticResource GoldBrush}" Foreground="#000000" CornerRadius="6" Padding="16,6"/>
                </StackPanel>
                <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Foreground="#FFD54F" FontStyle="Italic" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="3" Text="Referral history" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,8,0,8"/>

        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="No referrals yet."
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontStyle="Italic"
                           Margin="0,8,0,0"
                           Visibility="{x:Bind ViewModel.EmptyMessageVisibility, Mode=OneWay}"/>
                <ItemsControl ItemsSource="{x:Bind ViewModel.History, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="models:ReferralHistoryItem">
                            <Border Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="14" Margin="0,0,0,8" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Spacing="2">
                                        <TextBlock Text="{x:Bind FriendName}" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{x:Bind EventTitle}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{x:Bind FormattedDate}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
