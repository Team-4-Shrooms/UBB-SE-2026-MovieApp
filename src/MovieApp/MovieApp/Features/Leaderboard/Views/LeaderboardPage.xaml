<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MovieApp.Features.Leaderboard.Views.LeaderboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MovieApp.Features.Leaderboard.ViewModels"
    mc:Ignorable="d"
    Background="{StaticResource AppBackgroundBrush}">

    <ScrollViewer Padding="28" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="20">

            <!-- Header -->
            <StackPanel Spacing="4">
                <TextBlock Text="Leaderboard"
                           FontSize="28" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="Top users ranked by total points."
                           FontSize="14"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>

            <!-- Column headers -->
            <Grid Padding="16,0" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="48"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="110"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="#"
                           FontSize="12" FontWeight="SemiBold" CharacterSpacing="60"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="USER"
                           FontSize="12" FontWeight="SemiBold" CharacterSpacing="60"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Grid.Column="2" Text="TOTAL PTS"
                           FontSize="12" FontWeight="SemiBold" CharacterSpacing="60"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Right"/>
                <TextBlock Grid.Column="3" Text="WEEKLY"
                           FontSize="12" FontWeight="SemiBold" CharacterSpacing="60"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Right"/>
            </Grid>

            <!-- Ranking list -->
            <ItemsRepeater ItemsSource="{x:Bind ViewModel.Ranking, Mode=OneWay}">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="vm:RankedEntry">
                        <Border Background="{x:Bind RankBackground}"
                                BorderBrush="{StaticResource CardBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="16,14"
                                Margin="0,0,0,6">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="48"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="110"/>
                                </Grid.ColumnDefinitions>

                                <!-- Rank badge -->
                                <Border Grid.Column="0"
                                        Width="34" Height="34"
                                        CornerRadius="17"
                                        BorderBrush="{x:Bind RankForeground}"
                                        BorderThickness="1.5"
                                        HorizontalAlignment="Center">
                                    <TextBlock Text="{x:Bind RankDisplay}"
                                               FontSize="14" FontWeight="Bold"
                                               Foreground="{x:Bind RankForeground}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>

                                <!-- Username -->
                                <StackPanel Grid.Column="1"
                                            Orientation="Horizontal"
                                            Spacing="10"
                                            VerticalAlignment="Center">
                                    <FontIcon Glyph="&#xE77B;" FontSize="15"
                                              Foreground="{StaticResource TextSecondaryBrush}"/>
                                    <TextBlock Text="{x:Bind Stats.User.Username, FallbackValue='—'}"
                                               FontSize="15" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               VerticalAlignment="Center"/>
                                </StackPanel>

                                <!-- Total points -->
                                <TextBlock Grid.Column="2"
                                           Text="{x:Bind Stats.TotalPoints}"
                                           FontSize="16" FontWeight="Bold"
                                           Foreground="{x:Bind PointsForeground}"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"/>

                                <!-- Weekly score -->
                                <TextBlock Grid.Column="3"
                                           Text="{x:Bind Stats.WeeklyScore}"
                                           FontSize="15"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>

        </StackPanel>
    </ScrollViewer>
</Page>
