<Page
    x:Class="MovieApp.Features.Screenings.Views.ScreeningPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MovieApp.DataLayer.Models"
    xmlns:logicmodels="using:MovieApp.Logic.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left Pane: Screenings List -->
        <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="0,0,1,0" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="Screenings" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
                <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Screenings, Mode=OneWay}" 
                          SelectedItem="{x:Bind ViewModel.SelectedScreening, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Screening">
                            <StackPanel Margin="0,5">
                                <TextBlock Text="{x:Bind ScreeningTime}" FontWeight="SemiBold" />
                                <TextBlock Text="{x:Bind EventId}" Foreground="Gray" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- Right Pane: Seat Map -->
        <Grid Grid.Column="1" Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Select a Seat" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" />

            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <GridView ItemsSource="{x:Bind ViewModel.Seats, Mode=OneWay}"
                          SelectedItem="{x:Bind ViewModel.SelectedSeat, Mode=TwoWay}"
                          SelectionMode="Single">
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="logicmodels:Seat">
                            <Grid Width="60" Height="60" Margin="5" Background="LightGray">
                                <TextBlock Text="{x:Bind Row}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Book Selected Seat" Command="{x:Bind ViewModel.BookSeatCommand}" Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
