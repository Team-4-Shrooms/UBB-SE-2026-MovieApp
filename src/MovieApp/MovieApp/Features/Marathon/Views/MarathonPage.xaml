<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MovieApp.Features.Marathon.Views.MarathonPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MovieApp.DataLayer.Models"
    xmlns:converters="using:MovieApp.Features.Shared.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <ScrollViewer Padding="24">
        <Grid RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Row 0: Title -->
            <StackPanel Grid.Row="0" Spacing="4">
                <TextBlock Text="Movie Marathons" FontSize="28" FontWeight="Bold"/>
                <TextBlock Text="Browse this week's themed marathons and track your progress."
                           FontSize="14" Opacity="0.6"/>
            </StackPanel>

            <!-- Row 1: Loading indicator -->
            <ProgressRing Grid.Row="1"
                          IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                          Width="36" Height="36"
                          HorizontalAlignment="Center"/>

            <!-- Row 2: Marathon list -->
            <Border Grid.Row="2"
                    Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource CardBorderBrush}"
                    BorderThickness="1" CornerRadius="10" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Available Marathons" FontSize="16" FontWeight="SemiBold"/>
                    <ListView ItemsSource="{x:Bind ViewModel.Marathons, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.SelectedMarathon, Mode=TwoWay}"
                              SelectionMode="Single">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:Marathon">
                                <Border Margin="0,4" Padding="12,10" CornerRadius="8"
                                        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1">
                                    <StackPanel Spacing="4">
                                        <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" FontSize="14"/>
                                        <TextBlock Text="{x:Bind Theme}" FontSize="12" Opacity="0.6"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Row 3: Progress + Enroll -->
            <Border Grid.Row="3"
                    Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource CardBorderBrush}"
                    BorderThickness="1" CornerRadius="10" Padding="16"
                    Visibility="{x:Bind ViewModel.SelectedMarathon, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel Spacing="12">
                    <StackPanel Spacing="6">
                        <TextBlock Text="{x:Bind ViewModel.ProgressText, Mode=OneWay}" FontWeight="SemiBold"/>
                        <ProgressBar Minimum="0" Maximum="1"
                                     Value="{x:Bind ViewModel.CompletionPercentage, Mode=OneWay}"
                                     Height="8"/>
                    </StackPanel>
                    <Button Content="Enroll in Marathon"
                            Command="{x:Bind ViewModel.EnrollCommand}"
                            HorizontalAlignment="Stretch"
                            Style="{ThemeResource AccentButtonStyle}"
                            Visibility="{x:Bind ViewModel.IsEnrolled, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <StackPanel Orientation="Horizontal" Spacing="8"
                                Visibility="{x:Bind ViewModel.IsEnrolled, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <FontIcon Glyph="&#xE73E;" FontSize="16" Foreground="#4CAF50"/>
                        <TextBlock Text="You are enrolled in this marathon" FontSize="13" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Border>

        </Grid>
    </ScrollViewer>
</Page>
